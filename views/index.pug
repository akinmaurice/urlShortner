extends layout

include mixins/_urlForm

block content
  .container.h-100
    .row.h-100.justify-content-center.align-items-center
      .col-lg-12.text-center
        .row.justify-c
        h1.text-red= h.siteName
        +urlForm()
        if shortenedUrl
          p
            a(href=`${shortenedUrl}` target="_blank").urlLink= shortenedUrl
